Flask==2.3.2
Flask-SocketIO==5.3.2
python-dotenv==1.0.1
openai==1.40.6
numpy==1.24.3
scikit-learn==1.3.2
sqlite3==3.41.0  # Note: sqlite3 is included with Python's standard library; you don't need to install it separately.
pickle5==0.0.11  # For compatibility with pickle protocol if needed
